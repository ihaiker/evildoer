group 'la.renzhen.basis'
version '1.1.5'

apply from: "module.gradle"

subprojects {
    plugins.withType(MavenPlugin) {
        uploadArchives {
            configuration = configurations.archives
            repositories {
                mavenDeployer {
                    pom.project {
                        url "https://github.com/ihaiker/evildoer.git"
                        inceptionYear "20180612"
                        scm {
                            connection 'scm:git:https://github.com/ihaiker/evildoer.git'
                            developerConnection 'scm:git:https://github.com/ihaiker/evildoer.git'
                            url 'https://github.com/ihaiker/evildoer.git'
                        }
                        developers {
                            developer {
                                id 'ihaiker'
                                name 'haiker'
                                email 'wo@reznzhen.la'
                            }
                        }
                    }
                }
            }
        }
    }
}